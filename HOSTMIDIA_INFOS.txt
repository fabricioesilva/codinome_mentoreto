# Host midia criou uma aplicacao em python para meu uso
# Foi criada na pasta Sistema
# Alguns arquivos e pastas foram criadas nesta pasta para uso na Hostmidia
# Tais arquivos e pastas não são conteúdos do Django
# Não serão iginorados no .gitignore. Serão mantidos cópias neste repositório para fins de pull/push
__pycache__/
cgi-bin/
public/
tmp/
.well-known/
passenger_wsgi.py
.htaccess
.mypy_cache






python3 manage.py shell

from usuarios.models import CustomUser
users = CustomUser.objects.all()
users

// Iniciar serviço ssh no linux
eval "$(ssh-agent -s)"

// Encontrar arquivo no linux pelo nome 
find / -name "parte do arquivo" -print

// Alterar configurações do SSH, como porta de conexão
sudo nano /etc/ssh/sshd_config
sudo service ssh restart
sudo systemctl restart ssh

// Configurando o Git para uso em uma porta específica
git remote add sishost ssh://expertzo@107.161.183.50:28041/home/expertzo/sisrepo

/// Acessando o terminal remoto
ssh -p 28041 expertzo@107.161.183.50 ou somente ssh expertzo@107.161.183.50
cd sistema
source /home/expertzo/virtualenv/sistema/3.9/bin/activate && cd /home/expertzo/sistema
git stash
git pull sisrepo master
rm -r static
python3 manage.py collectstatic --clear
python3 manage.py migrate
cp media/favicon.ico static/
Restart Python APP em CPanel


git add .
git commit -m 'correcao arquivo simulado, favicon, errorspages'
git push origin master
git push sisrepo master


